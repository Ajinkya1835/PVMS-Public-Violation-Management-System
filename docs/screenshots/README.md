# Screenshots

Preview images for the GitHub README (SVG wireframes). Replace with real PNG captures for a stronger portfolio:

1. Run the app locally (`npm run dev:frontend` + `npm run dev:backend`).
2. Log in as each role (see [TESTING.md](../TESTING.md)).
3. Save PNGs here as `login.png`, `citizen.png`, `owner.png`, `officer.png`.
4. Update root `README.md` image links from `.svg` to `.png`.

**Recommended size:** 1200×750 px or similar 16:10 aspect ratio.
