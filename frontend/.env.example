# Copy to .env — required for map features
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# API URL (production / Vercel) — leave unset for local dev (uses localhost:5000)
# VITE_API_URL=https://your-api.onrender.com
